(* Package *)

DefineObjectType[Model[Maintenance, ParameterizeCover], {
  Description->"Definition of a set of parameters for a maintenance protocol that parameterizes new covers received via the inventory system.",
  CreatePrivileges->None,
  Cache->Session,
  Fields -> {

  }
}];
