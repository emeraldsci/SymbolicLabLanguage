(* ::Package:: *)

(* Package *)

DefineObjectType[Model[Maintenance, ReceivePackage], {
	Description->"Definition of a set of parameters for a maintenance protocol that receives items in the inventory.",
	CreatePrivileges->None,
	Cache->Session,
	Fields -> {
		
	}
}];
