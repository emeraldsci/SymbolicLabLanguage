(* ::Package:: *)

(* ::Text:: *)
(*\[Copyright] 2011-2025 Emerald Cloud Lab, Inc.*)

DefineObjectType[Object[Calibration, Sensor, ReedSwitch], {
  Description->"A calibration object used to convert the raw output of a Reed Switch monitoring an enclosure into an indication as to if the enclosure is open or closed.",
  CreatePrivileges->None,
  Cache->Session,
  Fields -> {
  }
}];